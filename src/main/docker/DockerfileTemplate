FROM openjdk

VOLUME /tmp
# ADD maven/spring-boot-docker-1.0.0-Beta.jar myapp.jar
ADD maven/${fileName}.jar ${fileName}.jar
RUN sh -c "touch /${fileName}.jar"
ENTRYPOINT ["java","-Djava.security.egd=file:/dev/./urandom","-jar","/${fileName}.jar"]
